import React from "react";

const operation = (num1,num2,op) => {
    debugger
    let numValue = Number(num1);
    let prevValue = Number(num2);
    let value = (numValue ,op,prevValue )

    return value;

}

export default operation;