import { Component } from "react";

export default class Operators extends Component {

    render() {
        return(
            <div>
                OPERATORS (+, -, etc.)
            </div>
        )
    }
}